version: 1

sources:
  demo_cli:
    type: cli
    executable: python3
    discovery:
      mode: manifest
      manifest: ./echo-manifest.yaml

mounts:
  - id: demo-echo
    source: demo_cli
    operation: echo_json
    command:
      path: [demo, echo]
    help:
      summary: Echo structured JSON
