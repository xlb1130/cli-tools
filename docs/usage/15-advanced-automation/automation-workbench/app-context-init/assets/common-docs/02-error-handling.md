# 错误处理

## 目标

让自动化在出错时有一致行为：可识别、可降级、可恢复、可上报。

## 错误分类

- 输入错误：参数缺失、格式错误、上下文不足。
- 环境错误：依赖未安装、配置缺失、权限不足。
- 外部系统错误：网络失败、接口异常、超时、限流。
- 业务错误：查询为空、状态冲突、前置条件不成立。
- 内部错误：脚本缺陷、逻辑异常、未处理分支。

## 处理策略

- 可重试错误：指数退避，限制次数。
- 可降级错误：切只读、切小范围、切替代路径。
- 不可恢复错误：立即停止并上报。

## 输出要求

- 错误类型。
- 影响范围。
- 是否重试过。
- 当前建议动作。
