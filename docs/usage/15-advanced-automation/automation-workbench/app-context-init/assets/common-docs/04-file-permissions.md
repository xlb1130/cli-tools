# 文件权限

## 目标

限制自动化对本地文件系统的影响范围，避免误覆盖、误删除和权限漂移。

## 基本规则

- 优先读，谨慎写，禁止无确认删除。
- 受管文件和用户文件必须区分。
- 脚本目录、配置目录、报告目录的写入边界必须明确。

## 覆盖策略

- 模板首次分发允许创建。
- 受管区块允许增量更新。
- 用户自定义区块禁止覆盖。

## 执行权限

- 仅脚本文件保留执行权限。
- 文档和模板默认不可执行。
