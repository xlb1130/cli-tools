# Diamond 配置使用规范

## 目标

确保配置中心是可治理资产，而不是临时救火的隐形代码层。

## 命名规范

- 配置名体现业务域、环境和用途。
- 禁止使用无语义缩写。
- 同一类配置保持一致命名模式。

## 发布原则

- 配置变更与代码变更一样需要评审。
- 变更必须记录影响范围和回滚值。
- 生产配置默认灰度或分批生效。

## 风险控制

- 高风险配置要有双人复核。
- 默认值必须经过验证，不能依赖“理论上不会缺”。
- 敏感信息不要散落在通用配置项中。

## 回滚要求

- 能快速恢复上一个稳定值。
- 能识别配置值回滚后是否需要配套重启或缓存刷新。
