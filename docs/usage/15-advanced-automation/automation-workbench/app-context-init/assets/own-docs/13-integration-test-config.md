# 集成测试配置

## 目标

确保集成测试环境能稳定复现实战问题，而不是只在理想路径下通过。

## 配置范围

- 服务依赖。
- 数据准备。
- 账号权限。
- 开关状态。
- 外部接口替身或沙箱。

## 设计原则

- 环境尽可能接近真实拓扑。
- 测试配置必须可重建、可复用、可审计。
- 环境差异要被显式记录，不能靠口头传递。

## 检查重点

- 是否存在测试环境独有逻辑。
- 是否存在缺失依赖被 mock 掩盖。
- 是否有脏数据导致结果不稳定。
