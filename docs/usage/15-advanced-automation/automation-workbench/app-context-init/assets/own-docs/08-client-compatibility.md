# 客户端兼容性

## 目标

在多端、多版本、多灰度状态并存时，控制服务端变更对客户端行为的破坏风险。

## 兼容性矩阵

- 客户端版本维度。
- 平台维度：iOS、Android、Web、小程序、桌面端。
- 配置维度：新开关、旧开关、混合状态。
- 数据维度：新老字段、新老协议。

## 检查重点

- 字段新增是否对旧端安全。
- 字段删除是否造成旧端崩溃或空白页。
- 行为变更是否需要版本门槛。
- 错误处理是否对旧端友好。

## 设计原则

- 向后兼容优先。
- 新能力优先通过新增字段或新增能力表达，而非覆写旧语义。
- 客户端无法及时升级时，服务端必须承担兼容责任。
